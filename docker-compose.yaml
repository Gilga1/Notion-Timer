services:
  focus-timer:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - focus-timer-data:/app/data
    restart: unless-stopped

volumes:
  focus-timer-data:
